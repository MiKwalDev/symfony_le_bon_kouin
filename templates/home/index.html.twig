{% extends 'base.html.twig' %}

{% block title %}Le Bon Kouin - Home Page{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1 class="text-2xl font-bold text-center">Bienvenue sur Le Bon Kouin !</h1>

    <section class="mt-5">
        <h2 class="text-xl font-bold text-center">Annonces disponibles:</h2>
        <div class="flex">
            {% for post in posts %}
                <a href="/post/{{post.id}}" class="mx-5 p-2 border-2 mt-2 w-3/6">
                    <article>
                        <h3>{{ post.title }}</h3>
                        <p>{{ post.price }} €</p>
                    </article>
                </a>
            {% endfor %}
        </div>
    </section>
</div>
{% endblock %}
